include "test.conf"
# DO NOT add any line before this. Insert them below this part.

# Persistent unit should be used by your JPA provider. 
jpa.default=testPersistenceUnit

db.default.driver=org.h2.Driver
#db.default.url="jdbc:h2:mem:play-test"
db.default.url="jdbc:h2:mem:play;INIT=create schema if not exists public;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=FALSE"
db.default.username=sa
db.default.password=""